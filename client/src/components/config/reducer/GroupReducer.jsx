const initialState = {
  group: [],
};

export const groupReducer = (state = initialState, action) => {
  if (action.type === "SUCCESS_ADD_GROUP") {
    return state;
  } else {
    return state;
  }
};
